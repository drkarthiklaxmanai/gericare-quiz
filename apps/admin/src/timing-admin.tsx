import React,{useEffect,useState} from 'react';import{createRoot}from'react-dom/client';import{createClient}from'@supabase/supabase-js';import'./styles.css';
const url=import.meta.env.VITE_SUPABASE_URL as string;const key=import.meta.env.VITE_SUPABASE_ANON_KEY as string;const configuredEvent=import.meta.env.VITE_EVENT_ID as string|undefined;const sb=createClient(url,key);
function App(){const[eventId,setEventId]=useState(configuredEvent??'');const[prelim,setPrelim]=useState(90);const[finalSec,setFinalSec]=useState(600);const[busy,setBusy]=useState(false);const[msg,setMsg]=useState('');
const load=async()=>{let eid=eventId;if(!eid){const{data,error}=await sb.from('events').select('id').limit(1).maybeSingle();if(error)throw error;if(!data)throw Error('No event');eid=data.id;setEventId(eid)}const{data,error}=await sb.rpc('get_quiz_timers',{p_event_id:eid});if(error)throw error;setPrelim(Number(data.prelim_seconds??90));setFinalSec(Number(data.final_seconds??600))};useEffect(()=>{void load().catch(e=>setMsg(e.message))},[]);
const save=async()=>{setBusy(true);setMsg('');try{const{data,error}=await sb.rpc('set_quiz_timers',{p_event_id:eventId,p_prelim_seconds:prelim,p_final_seconds:finalSec});if(error)throw error;setMsg(`Saved. Prelims ${data.prelim_seconds}s · Final ${data.final_seconds}s`)}catch(e){setMsg(e instanceof Error?e.message:String(e))}finally{setBusy(false)}};
return <div className="app"><header className="topbar"><div><small>GERiCARE</small><h1>Quiz timers</h1></div><div className="pill">Server-authoritative</div></header><div className="wrap"><div className="card" style={{maxWidth:620,margin:'0 auto'}}><p className="hint">These durations are read by the backend when a new preliminary round or final starts. Changing them does not alter an attempt already in progress.</p><div className="section" style={{display:'grid',gap:18}}><label><b>Preliminary round</b><p className="hint">All 3 questions together. Allowed: 30 seconds to 15 minutes.</p><div style={{display:'flex',gap:10,alignItems:'center'}}><input className="search" type="number" min={30} max={900} value={prelim} onChange={e=>setPrelim(Number(e.target.value))}/><span>seconds</span></div></label><label><b>Final</b><p className="hint">All final questions together. Allowed: 1 to 60 minutes.</p><div style={{display:'flex',gap:10,alignItems:'center'}}><input className="search" type="number" min={60} max={3600} value={finalSec} onChange={e=>setFinalSec(Number(e.target.value))}/><span>seconds</span></div></label>{msg&&<div className="pill">{msg}</div>}<div style={{display:'flex',gap:10,flexWrap:'wrap'}}><button className="btn primary" disabled={busy||prelim<30||prelim>900||finalSec<60||finalSec>3600} onClick={()=>void save()}>{busy?'Saving…':'Save timers'}</button><a className="btn" href="./">Back to Admin</a></div></div></div></div></div>}
createRoot(document.getElementById('root')!).render(<React.StrictMode><App/></React.StrictMode>);