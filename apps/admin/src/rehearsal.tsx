import React,{useEffect,useState}from'react';import{createRoot}from'react-dom/client';import{createClient}from'@supabase/supabase-js';import'./styles.css';
const url=import.meta.env.VITE_SUPABASE_URL as string|undefined,key=import.meta.env.VITE_SUPABASE_ANON_KEY as string|undefined;const sb=url&&key?createClient(url,key):null;
type EventRow={id:string;name:string;is_demo:boolean};type Summary={run_id:string;participants:number;rounds:number;preliminary_questions:number;final_questions:number;top10:any[];final:any[];sudden_death_forced:boolean;winner:string|null};
function App(){const[events,setEvents]=useState<EventRow[]>([]),[eventId,setEventId]=useState(''),[count,setCount]=useState(100),[force,setForce]=useState(true),[busy,setBusy]=useState(false),[message,setMessage]=useState('Ready'),[summary,setSummary]=useState<Summary|null>(null);const load=async()=>{if(!sb){setMessage('Supabase not configured');return}const{data,error}=await sb.from('events').select('id,name,is_demo').eq('is_demo',true).order('created_at',{ascending:false});if(error){setMessage(error.message);return}setEvents(data??[]);if(!eventId&&data?.[0])setEventId(data[0].id)};useEffect(()=>{void load()},[]);const run=async()=>{if(!sb||!eventId)return;setBusy(true);setMessage('Running full rehearsal…');setSummary(null);const{data,error}=await sb.rpc('run_demo_rehearsal',{p_event_id:eventId,p_participant_count:count,p_force_sudden_death:force});setBusy(false);if(error){setMessage(error.message);return}setSummary(data as Summary);setMessage('Rehearsal completed')};return <div className="app"><header><div><small>GERiCARE • DEMO MODE</small><h1>Rehearsal Console</h1></div><span>{message}</span></header><main style={{gridTemplateColumns:'1fr'}}><section><div className="card"><h2>Run isolated conference simulation</h2><p className="muted">Uses demo-only tables. No real participant, attempt, score or leaderboard records are modified.</p><div className="fields"><label>Demo event<select value={eventId} onChange={e=>setEventId(e.target.value)}><option value="">Select demo event</option>{events.map(e=><option key={e.id} value={e.id}>{e.name}</option>)}</select></label><label>Participants<input type="number" min="10" max="1000" value={count} onChange={e=>setCount(+e.target.value)}/></label><label>Force sudden death<input type="checkbox" checked={force} onChange={e=>setForce(e.target.checked)}/></label></div><button className="primary" disabled={busy||!eventId} onClick={run}>{busy?'Running…':'Run Full Rehearsal'}</button></div>{summary&&<><div className="card"><h2>Validation summary</h2><div className="stats"><div><b>{summary.participants}</b><span>Participants</span></div><div><b>{summary.rounds}</b><span>Rounds</span></div><div><b>{summary.preliminary_questions}</b><span>Prelim questions</span></div><div><b>{summary.final_questions}</b><span>Final questions</span></div></div><p><strong>Winner:</strong> {summary.winner??'—'}</p><p><strong>Sudden death:</strong> {summary.sudden_death_forced?'Forced and resolved':'Not forced'}</p></div><div className="card"><h2>Top 10 after Best-5</h2>{summary.top10?.map((x:any)=><div className="assigned" key={x.rank}><b>#{x.rank}</b><span>{x.name}</span><strong>{x.best5_score} pts</strong></div>)}</div><div className="card"><h2>Grand Final result</h2>{summary.final?.map((x:any)=><div className="assigned" key={x.rank}><b>#{x.rank}</b><span>{x.name}</span><strong>{x.score} pts</strong></div>)}</div></>}</section></main></div>};createRoot(document.getElementById('root')!).render(<React.StrictMode><App/></React.StrictMode>);
