import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import OtpGate from './OtpGate'

createRoot(document.getElementById('root')!).render(<StrictMode><OtpGate /></StrictMode>)
